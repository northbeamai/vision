<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vision Pro - تحول رؤيتك إلى هوية مؤثرة</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700;800&family=Tajawal:wght@400;500;700;800&display=swap" rel="stylesheet">
    <style>
        /* استيراد الخطوط وإعدادات عامة */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-color: #2563eb;
            --secondary-color: #1e40af;
            --accent-color: #3b82f6;
            --text-color: #1f2937;
            --light-text: #6b7280;
            --bg-light: #f8fafc;
            --bg-white: #ffffff;
            --shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
            --radius: 12px;
            --transition: all 0.3s ease;
            --success: #10b981;
            --whatsapp: #25D366;
            --gold: #FFD700;
        }

        body {
            font-family: 'Cairo', 'Tajawal', sans-serif;
            line-height: 1.6;
            color: var(--text-color);
            background-color: var(--bg-white);
            direction: rtl;
        }

        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* شعار Vision Pro المخصص */
        .vision-pro-logo {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            margin-bottom: 40px;
            position: relative;
        }

        .logo-main {
            font-family: 'Tajawal', sans-serif;
            font-size: 4rem;
            font-weight: 800;
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: -1px;
            margin-bottom: 10px;
            position: relative;
            text-align: center;
        }

        .logo-main::after {
            content: '';
            position: absolute;
            bottom: -5px;
            right: 50%;
            transform: translateX(50%);
            width: 120px;
            height: 3px;
            background: linear-gradient(90deg, var(--gold) 0%, var(--accent-color) 100%);
            border-radius: 2px;
        }

        .logo-subtitle {
            font-family: 'Cairo', sans-serif;
            font-size: 1.3rem;
            font-weight: 600;
            color: var(--light-text);
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-top: 5px;
        }

        /* قسم البداية (Hero Section) */
        .hero-section {
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            color: white;
            text-align: center;
            padding: 80px 20px 100px;
            position: relative;
            overflow: hidden;
        }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 50%, rgba(59, 130, 246, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(255, 215, 0, 0.05) 0%, transparent 50%);
        }

        .hero-section h1 {
            font-size: 2.8rem;
            font-weight: 800;
            margin-bottom: 20px;
            line-height: 1.2;
            position: relative;
            color: white;
        }

        .hero-section p {
            font-size: 1.3rem;
            margin-bottom: 40px;
            opacity: 0.9;
            max-width: 700px;
            margin-right: auto;
            margin-left: auto;
            color: #e2e8f0;
        }

        /* أزرار CTA */
        .cta-button {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            background: var(--accent-color);
            color: white;
            padding: 16px 40px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            transition: var(--transition);
            border: 2px solid transparent;
            cursor: pointer;
            margin: 10px;
            min-width: 200px;
        }

        .cta-button:hover {
            background: var(--secondary-color);
            transform: translateY(-3px);
            box-shadow: var(--shadow);
        }

        .whatsapp-button {
            background: var(--whatsapp);
        }

        .whatsapp-button:hover {
            background: #1da851;
            box-shadow: 0 10px 25px rgba(37, 211, 102, 0.3);
        }

        .contact-button {
            background: transparent;
            border: 2px solid var(--accent-color);
            color: var(--accent-color);
        }

        .contact-button:hover {
            background: var(--accent-color);
            color: white;
        }

        /* قسم الخدمات */
        .services-section {
            padding: 100px 20px;
            background: var(--bg-light);
        }

        .section-title {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 60px;
            color: var(--text-color);
            position: relative;
            font-weight: 800;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -15px;
            right: 50%;
            transform: translateX(50%);
            width: 80px;
            height: 4px;
            background: linear-gradient(90deg, var(--gold) 0%, var(--primary-color) 100%);
            border-radius: 2px;
        }

        .service-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 40px;
            margin-top: 40px;
        }

        .service-card {
            background: var(--bg-white);
            padding: 40px 30px;
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            transition: var(--transition);
            text-align: center;
            position: relative;
            overflow: hidden;
            border-top: 4px solid transparent;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(37, 99, 235, 0.03) 0%, rgba(255, 215, 0, 0.02) 100%);
            opacity: 0;
            transition: var(--transition);
        }

        .service-card:hover::before {
            opacity: 1;
        }

        .service-card:hover {
            transform: translateY(-10px);
            border-top-color: var(--accent-color);
        }

        .service-icon {
            font-size: 3rem;
            margin-bottom: 25px;
            color: var(--primary-color);
            height: 80px;
            width: 80px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: auto;
            margin-left: auto;
            background: linear-gradient(135deg, rgba(37, 99, 235, 0.1) 0%, rgba(255, 215, 0, 0.1) 100%);
            border-radius: 50%;
        }

        .service-card h3 {
            font-size: 1.6rem;
            margin-bottom: 20px;
            color: var(--text-color);
            font-weight: 700;
        }

        .service-card p {
            color: var(--light-text);
            font-size: 1.1rem;
            line-height: 1.8;
        }

        /* قسم التواصل */
        .contact-section {
            padding: 100px 20px;
            background: linear-gradient(135deg, var(--bg-light) 0%, #e0f2fe 100%);
            text-align: center;
        }

        .contact-section p {
            font-size: 1.2rem;
            color: var(--light-text);
            margin-bottom: 50px;
            max-width: 600px;
            margin-right: auto;
            margin-left: auto;
        }

        .contact-info {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 30px;
            margin-bottom: 50px;
        }

        .contact-item {
            background: var(--bg-white);
            padding: 30px 25px;
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            transition: var(--transition);
            min-width: 250px;
            text-align: center;
            border-bottom: 4px solid transparent;
        }

        .contact-item:hover {
            transform: translateY(-5px);
            border-bottom-color: var(--accent-color);
        }

        .contact-icon {
            font-size: 2.5rem;
            margin-bottom: 15px;
            color: var(--primary-color);
            height: 70px;
            width: 70px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: auto;
            margin-left: auto;
            background: rgba(37, 99, 235, 0.1);
            border-radius: 50%;
        }

        .contact-item h3 {
            font-size: 1.3rem;
            margin-bottom: 10px;
            color: var(--text-color);
            font-weight: 600;
        }

        .contact-item a, .contact-item p {
            color: var(--light-text);
            text-decoration: none;
            font-size: 1.1rem;
            transition: var(--transition);
            direction: ltr;
            display: block;
        }

        .contact-item a:hover {
            color: var(--primary-color);
        }

        .whatsapp-contact {
            border-bottom-color: var(--whatsapp);
        }

        .whatsapp-contact .contact-icon {
            color: var(--whatsapp);
            background: rgba(37, 211, 102, 0.1);
        }

        form {
            max-width: 600px;
            margin: 0 auto;
            background: var(--bg-white);
            padding: 50px;
            border-radius: var(--radius);
            box-shadow: var(--shadow);
        }

        input, textarea {
            width: 100%;
            padding: 18px 25px;
            margin-bottom: 25px;
            border: 2px solid #e5e7eb;
            border-radius: var(--radius);
            font-family: 'Cairo', sans-serif;
            font-size: 1.1rem;
            transition: var(--transition);
            background: var(--bg-light);
        }

        input:focus, textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            background: var(--bg-white);
            box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
        }

        textarea {
            height: 180px;
            resize: vertical;
        }

        form .cta-button {
            width: 100%;
            padding: 20px;
            font-size: 1.2rem;
            border: none;
            margin: 0;
        }

        /* التذييل */
        footer {
            background: #0f172a;
            color: white;
            padding: 60px 20px 30px;
        }

        .footer-content {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            align-items: flex-start;
            max-width: 1200px;
            margin: 0 auto 40px;
            gap: 40px;
        }

        .footer-logo-section {
            flex: 1;
            min-width: 300px;
        }

        .footer-logo {
            font-family: 'Tajawal', sans-serif;
            font-size: 2.2rem;
            font-weight: 800;
            margin-bottom: 15px;
            background: linear-gradient(135deg, var(--accent-color) 0%, var(--gold) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .footer-tagline {
            color: #cbd5e1;
            font-size: 1rem;
            margin-bottom: 20px;
            font-weight: 300;
        }

        .social-links {
            display: flex;
            gap: 15px;
            margin-top: 20px;
        }

        .social-icon {
            width: 45px;
            height: 45px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.2rem;
            transition: var(--transition);
            text-decoration: none;
        }

        .social-icon:hover {
            background: var(--accent-color);
            transform: translateY(-3px);
        }

        .whatsapp-icon:hover {
            background: var(--whatsapp);
        }

        .email-icon:hover {
            background: #ea4335;
        }

        .phone-icon:hover {
            background: var(--success);
        }

        .footer-bottom {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .footer-bottom p {
            font-size: 1rem;
            opacity: 0.8;
        }

        /* تأثيرات إضافية */
        .service-card:nth-child(1) .service-icon {
            color: #3b82f6;
        }

        .service-card:nth-child(2) .service-icon {
            color: #10b981;
        }

        .service-card:nth-child(3) .service-icon {
            color: #8b5cf6;
        }

        /* التجاوب */
        @media (max-width: 768px) {
            .logo-main {
                font-size: 3rem;
            }
            
            .hero-section h1 {
                font-size: 2.2rem;
            }
            
            .hero-section p {
                font-size: 1.1rem;
            }
            
            .section-title {
                font-size: 2rem;
            }
            
            .service-grid {
                grid-template-columns: 1fr;
                gap: 30px;
            }
            
            .contact-info {
                flex-direction: column;
                align-items: center;
            }
            
            .contact-item {
                width: 100%;
                max-width: 400px;
            }
            
            form {
                padding: 30px 20px;
            }
            
            input, textarea {
                padding: 15px 20px;
            }
            
            .footer-content {
                flex-direction: column;
                text-align: center;
            }
            
            .footer-logo-section {
                min-width: auto;
            }
        }

        @media (max-width: 480px) {
            .hero-section {
                padding: 60px 20px 80px;
            }
            
            .logo-main {
                font-size: 2.5rem;
            }
            
            .logo-subtitle {
                font-size: 1rem;
            }
            
            .hero-section h1 {
                font-size: 1.8rem;
            }
            
            .cta-button {
                padding: 14px 25px;
                font-size: 1rem;
                min-width: auto;
                width: 100%;
                margin: 5px 0;
            }
            
            .services-section,
            .contact-section {
                padding: 60px 20px;
            }
            
            .contact-item {
                min-width: auto;
                width: 100%;
            }
        }
    </style>
</head>
<body>

    <!-- قسم البداية (Hero Section) -->
    <header class="hero-section">
        <div class="container">
            <div class="vision-pro-logo">
                <div class="logo-main">Vision Pro</div>
                <div class="logo-subtitle">FROM VISION TO BRAND</div>
            </div>
            
            <h1>تحول رؤيتك إلى هوية وهويتك إلى علامة مؤثرة</h1>
            <p>نقدم حلولاً متكاملة في التصميم، البراندينغ، وبناء المواقع الإلكترونية الحديثة التي تبرز هويتك وتنقل رؤيتك إلى الواقع.</p>
            <div class="hero-buttons">
                <a href="#services" class="cta-button">
                    <i class="fas fa-rocket"></i> اكتشف خدماتنا
                </a>
                <a href="https://wa.me/70161602" target="_blank" class="cta-button whatsapp-button">
                    <i class="fab fa-whatsapp"></i> تواصل عبر واتساب
                </a>
                <a href="#contact" class="cta-button contact-button">
                    <i class="fas fa-envelope"></i> اتصل بنا
                </a>
            </div>
        </div>
    </header>

    <!-- قسم الخدمات (Services Section) -->
    <section id="services" class="services-section">
        <div class="container">
            <h2 class="section-title">خدماتنا المتكاملة</h2>
            <div class="service-grid">
                <div class="service-card">
                    <div class="service-icon">
                        <i class="fas fa-palette"></i>
                    </div>
                    <h3>تصميم الهوية البصرية</h3>
                    <p>نصمم شعارات وهوية بصرية فريدة تعكس قيم علامتك التجارية وتجعلها لا تُنسى في أذهان العملاء، مع تقديم دليل هوية متكامل.</p>
                </div>
                <div class="service-card">
                    <div class="service-icon">
                        <i class="fas fa-code"></i>
                    </div>
                    <h3>بناء المواقع الإلكترونية</h3>
                    <p>نطور مواقع ويب سريعة، متجاوبة، ومحسنة لتجربة المستخدم ومحركات البحث لضمان وصول أوسع ووجود رقمي مؤثر.</p>
                </div>
                <div class="service-card">
                    <div class="service-icon">
                        <i class="fas fa-chart-line"></i>
                    </div>
                    <h3>استشارات براندينغ</h3>
                    <p>نقدم استراتيجيات تسويقية متكاملة لضمان حضور قوي ومؤثر في السوق وتحقيق النمو المستدام لعلامتك التجارية.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- قسم التواصل (Contact Section) -->
    <section id="contact" class="contact-section">
        <div class="container">
            <h2 class="section-title">تواصل معنا</h2>
            <p>ابدأ رحلة بناء علامتك التجارية اليوم. نحن بانتظارك لتقديم أفضل الحلول التي تناسب احتياجاتك وتحقق أهدافك.</p>
            
            <div class="contact-info">
                <div class="contact-item">
                    <div class="contact-icon">
                        <i class="fas fa-phone-alt"></i>
                    </div>
                    <h3>الهاتف</h3>
                    <a href="tel:+70161602">+70161602</a>
                </div>
                
                <div class="contact-item whatsapp-contact">
                    <div class="contact-icon">
                        <i class="fab fa-whatsapp"></i>
                    </div>
                    <h3>واتساب</h3>
                    <a href="https://wa.me/70161602" target="_blank">ارسل رسالة على واتساب</a>
                </div>
                
                <div class="contact-item">
                    <div class="contact-icon">
                        <i class="fas fa-envelope"></i>
                    </div>
                    <h3>البريد الإلكتروني</h3>
                    <a href="mailto:hassan.muslmani@icloud.com">hassan.muslmani@icloud.com</a>
                </div>
            </div>
            
            <form id="contact-form">
                <input type="text" placeholder="اسمك الكريم" required>
                <input type="email" placeholder="بريدك الإلكتروني" required>
                <input type="tel" placeholder="رقم هاتفك (اختياري)">
                <textarea placeholder="كيف يمكننا مساعدتك؟ ما هي احتياجاتك وأهدافك؟"></textarea>
                <button type="submit" class="cta-button">
                    <i class="fas fa-paper-plane"></i> إرسال الرسالة
                </button>
            </form>
        </div>
    </section>

    <!-- التذييل (Footer) -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-logo-section">
                    <div class="footer-logo">Vision Pro</div>
                    <div class="footer-tagline">FROM VISION TO BRAND</div>
                    <p style="color: #94a3b8; margin-bottom: 20px;">تحويل الرؤى إلى علامات تجارية مؤثرة منذ 2025</p>
                </div>
                
                <div>
                    <h3 style="color: white; margin-bottom: 20px; font-size: 1.3rem;">طرق التواصل</h3>
                    <div class="social-links">
                        <a href="https://wa.me/70161602" target="_blank" class="social-icon whatsapp-icon" title="واتساب">
                            <i class="fab fa-whatsapp"></i>
                        </a>
                        <a href="mailto:hassan.muslmani@icloud.com" class="social-icon email-icon" title="البريد الإلكتروني">
                            <i class="fas fa-envelope"></i>
                        </a>
                        <a href="tel:+70161602" class="social-icon phone-icon" title="الهاتف">
                            <i class="fas fa-phone-alt"></i>
                        </a>
                    </div>
                </div>
            </div>
            
            <div class="footer-bottom">
                <p>&copy; 2025 Vision Pro. جميع الحقوق محفوظة. | تصميم وتطوير بواسطة Vision Pro</p>
            </div>
        </div>
    </footer>

    <script>
        // إضافة تفاعل بسيط للنموذج
        document.getElementById('contact-form').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // هنا يمكنك إضافة كود إرسال النموذج إلى الخادم
            const submitBtn = this.querySelector('button[type="submit"]');
            const originalText = submitBtn.innerHTML;
            
            // عرض رسالة تحميل
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> جاري الإرسال...';
            submitBtn.disabled = true;
            
            // محاكاة الإرسال
            setTimeout(() => {
                alert('شكراً لتواصلك مع Vision Pro! سنرد عليك في أقرب وقت ممكن.');
                this.reset();
                submitBtn.innerHTML = originalText;
                submitBtn.disabled = false;
            }, 1500);
        });
        
        // إضافة تأثيرات scroll suave
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                
                const targetId = this.getAttribute('href');
                if(targetId === '#') return;
                
                const targetElement = document.querySelector(targetId);
                if(targetElement) {
                    window.scrollTo({
                        top: targetElement.offsetTop - 80,
                        behavior: 'smooth'
                    });
                }
            });
        });
        
        // إضافة تأثيرات للشعار عند التمرير
        window.addEventListener('scroll', function() {
            const logo = document.querySelector('.vision-pro-logo');
            const scrollPosition = window.scrollY;
            
            if (scrollPosition > 50) {
                logo.style.transform = 'scale(0.95)';
                logo.style.opacity = '0.9';
            } else {
                logo.style.transform = 'scale(1)';
                logo.style.opacity = '1';
            }
        });
    </script>

</body>
</html>